---
import { u } from "~/lib/url";
const { pathname } = Astro.url;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const stripBase = (p: string) => (base && p.startsWith(base) ? p.slice(base.length) || "/" : p);
const currentPath = stripBase(pathname);
const isHome = currentPath === "/" || currentPath === "";
const links = [
  { href: "/projeler", label: "Projeler" },
  { href: "/hakkimda", label: "Hakkımda" },
  { href: "/cv", label: "CV" },
  { href: "/iletisim", label: "İletişim" },
];
function isActive(href: string) {
  if (href === "/") return isHome;
  return currentPath === href || currentPath.startsWith(href + "/");
}
---

<header class="sticky top-0 z-50 bg-paper/85 backdrop-blur-md border-b border-line">
  <nav
    class="mx-auto flex items-center justify-between px-6 md:px-10 lg:px-14 py-4"
    style="max-width: var(--container-page)"
    aria-label="Ana gezinme"
  >
    <a
      href={u("/")}
      class="flex items-center group"
      aria-label="Ana sayfa — Berk Akkoç Tasarım & Mimarlık"
    >
      <img
        src={u("/logo.png")}
        alt="Berk Akkoç — Tasarım & Mimarlık"
        width="149"
        height="167"
        class="h-9 md:h-10 w-auto transition-opacity group-hover:opacity-80"
      />
    </a>

    <ul class="flex items-center gap-1 sm:gap-2 md:gap-4 text-sm">
      {links.map((l) => (
        <li>
          <a
            href={u(l.href)}
            class:list={[
              "px-2 sm:px-3 py-2 border-b transition-colors duration-200 hover:text-ink hover:border-ink",
              isActive(l.href)
                ? "text-ink border-ink"
                : "text-stone border-transparent",
            ]}
            aria-current={isActive(l.href) ? "page" : undefined}
          >
            {l.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
